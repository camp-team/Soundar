import { RemoveHtmlTagPipe } from './remove-html-tag.pipe';

describe('RemoveHtmlTagPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveHtmlTagPipe();
    expect(pipe).toBeTruthy();
  });
});
